import { Leaderboards } from './board';

export const metadata = {
  title: 'Leaderboards',
  description: 'Top coders at Codely, ranked by XP earned.',
};

export default function LeaderboardsPage() {
  return <Leaderboards />;
}
